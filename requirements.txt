# Runtime dependencies for update_voyage_output.py — kept in sync automatically
# by `update_voyage_output.py inventory` (edit example code, not this file).
# Install: uv venv --python 3.13 .venv && uv pip install --python .venv/bin/python -r requirements.txt
#
pyyaml
anthropic
openai
numpy
pymongo
langchain-community
langchain-text-splitters
pypdf
python-dotenv
Pillow
datasets
voyageai
